name: Deploy Cloud Storage

on:
  push:
    branches: [ server-version ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: 194.67.124.178
          username: mikhail
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            # Просто запускаем наш скрипт
            /opt/cloud_storage/deploy-and-restart.sh
